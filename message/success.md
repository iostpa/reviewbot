# Checks passed!

The checks for the pull request has passed! You should be good to go, though it is recommended to double check where you put the file (it should be in the `domains` directory), double check the records you put and make sure the username in the `owner` section is correct.