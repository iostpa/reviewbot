### Unauthorised
You are not authorised to modify the subdomain(s) in this pull request! You are either attempting to modify a subdomain which another user owns, or attempting to register a *nested subdomain* without having registered the root subdomain first.

If it is the latter scenario, please first register the root subdomain, `example.is-a.dev`, where the nested subdomain you are attempting to register here is `foo.example.is-a.dev`.